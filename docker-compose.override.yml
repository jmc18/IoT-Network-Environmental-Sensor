# Merged with docker-compose.yml by Docker Compose and Visual Studio.
# Keep local-only overrides here; do not commit secrets.
services:
  api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
